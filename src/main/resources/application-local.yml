spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen2.5-coder:3b

  application:
    name: LocalCoder

  datasource:
    url: jdbc:postgresql://localhost:5432/localcoder
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 233122

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    com.localdoc: DEBUG